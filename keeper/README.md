# keeper

Off-chain binary, three loops: Forecaster (5min), sentinel (1min), reporter
(1h). Full loop spec tracked internally, not in this repo.

Not started. Placeholder so the workspace layout matches the architecture
doc.
